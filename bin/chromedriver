#! /usr/bin/env ruby

require "webdrivers"

case ARGV.first
  when 'reset', 'uninstall', 'remove'
    Webdrivers::Chromedriver.reset
    %x(gem uninstall chromedriver)
  else
    Webdrivers::Chromedriver.install *ARGV
end

